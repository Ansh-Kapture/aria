pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Web search + scraping
duckduckgo-search>=6.0.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Vector store + local embeddings (no API key needed)
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0

# ── LLM provider SDKs (install the one(s) you need) ──────────────────────────

# Anthropic
anthropic>=0.40.0

# OpenAI / OpenAI-compatible (Ollama, vLLM, Together, Groq, etc.)
# pip install openai>=1.30.0
openai>=1.30.0

# Vertex AI (Google Cloud)
# pip install google-cloud-aiplatform>=1.60.0 google-auth>=2.29.0
# google-cloud-aiplatform>=1.60.0
# google-auth>=2.29.0
